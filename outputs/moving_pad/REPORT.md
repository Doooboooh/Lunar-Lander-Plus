# 拓展实验报告：移动着陆平台（Moving Landing Pad）

> 课程：神经网络 / 强化学习结课作业
> 基础环境：`LunarLander-v3`（gymnasium，Box2D 物理）
> 算法：基于 Stable-Baselines3 的 DQN / PPO / A2C 三种算法对比

---

## 一、任务背景与动机

基础 `LunarLander-v3` 任务中，着陆平台固定在画面正中，飞船只需"减速 + 回中"即可平稳
着陆。该任务的目标位置是**静态**的，智能体学到的策略本质是一个"回中器"。

现实中的着陆场景往往**目标位置是动态的**，例如：

- **海上火箭回收**：SpaceX 的无人船在海上随波浪漂移，火箭必须精准落到移动的甲板上；
- **航母舰载机降落**：航母甲板随海浪起伏、随航向移动；
- **无人机移动平台起降**：物流无人机降落到移动的运输车上。

这些场景的共同难点是：**智能体不仅要平稳减速，还要实时跟踪一个移动的目标，并在接触
瞬间与目标保持近零相对速度**，否则会被刚性碰撞弹飞。

因此本拓展把基础任务升级为**移动着陆平台**：在画面中引入一个左右往返移动的实体平台，
飞船必须跟踪它并平稳落到甲板上。这使任务从"静态目标控制"升级为"动态目标跟踪 + 精确
接触控制"，更充分地体现强化学习在复杂决策任务中的能力与边界。

---

## 二、环境设计

### 2.1 总体思路

在 `LunarLander-v3` 基础上做最小侵入式扩展，子类化 `LunarLander`：

- **新增实体平台**：一个 `KinematicBody`（运动学刚体）矩形甲板，悬在地表之上（不嵌在
  地下），沿水平方向做正弦往返移动；
- **观测扩展**：原 8 维观测 + 平台相对飞船的位置 2 维 = **10 维**；
- **奖励重塑**：把原版"相对画面中心"的 shaping 改为"相对平台当前位置"，并加入关键
  的"水平相对速度"惩罚项（见 2.4）；
- **动作空间不变**：仍是 4 个离散动作（不喷 / 左推 / 主推 / 右推）。

### 2.2 关键物理：让平台成为真正的实体

最核心的工程难点是让平台**真正挡住飞船**（飞船落到甲板顶面上，而不是穿过它落到地面）。

原版 `LunarLander` 中，飞船（lander / legs）的碰撞掩码 `maskBits = 0x001`，**只与地面
碰撞**。因此默认情况下新建的平台会被飞船穿透。

解决方法：reset 后修改飞船与腿的 `maskBits`，使其也包含平台的类别位：

```python
PAD_CAT = 0x0004
# 让 lander 与 legs 也碰平台（原版 maskBits=0x001 只碰地面）
for f in self.lander.fixtures:
    f.filterData.maskBits = 0x001 | PAD_CAT
for leg in self.legs:
    for f in leg.fixtures:
        f.filterData.maskBits = 0x001 | PAD_CAT
```

验证：飞船从平台上方无控制自由落体，会**稳稳停在甲板顶面上**（不穿透、不落地面）。

### 2.3 平台运动

平台中心 x 随时间正弦往返：

```python
ship_x = pad_x_amp * sin(omega * t / FPS + phase)
```

- `pad_x_amp`：移动幅度（米，相对画面中心）；
- `omega`：角频率，控制移动快慢；
- `phase`：随机相位，使每局轨迹不同。

平台的水平速度为 `pad_x_amp * omega * cos(...)`，峰值速度 = 幅度 × 频率。

**本实验参数**：`pad_x_amp = 2.2`（平台中心在画面 ±2.2 米间往返，覆盖画面大部分宽度，
近乎从最左到最右），`omega ∈ [1.2, 1.6]`（每局随机）。出界判定相应放宽到 `|x| < 4.0`，
避免飞船追平台时被误判出界。

### 2.4 奖励函数设计（逐步演化的核心）

奖励设计经历了三个版本，反映了我们对"平稳着陆"理解的深化：

**版本 1 — 目标条件 shaping**（基础）：

```python
shaping = -100 * sqrt(rel_x² + rel_y²)    # 飞船相对平台中心的距离
          - 100 * hypot(vel.x, vel.y)       # 飞船自身速度
          - 100 * abs(angle)                # 姿态偏离竖直
          + 10 * (双腿接地)
```

平稳落到平台上给 +120，坠毁 -100，落到地面（没上平台）-30。

**问题**：飞船学到了"减速 + 接近平台"，但在高速平台下，飞船带着**水平速度**撞到甲板，
被刚性碰撞**弹飞**，无法真正停住。

**版本 2 — 加入姿态约束**：把姿态惩罚从 100 提到 300，并在平稳判定中要求 `|angle| < 0.15`
（接近竖直）。这改善了着陆姿态，但**没解决弹飞**——因为弹飞的根因是水平相对速度，不是
姿态。

**版本 3（最终）— 加入水平相对速度惩罚**（关键）：

```python
pad_vx = pad_x_amp * omega * cos(omega * t / FPS + phase)   # 平台当前水平速度
rel_vx = vel.x - pad_vx                                       # 飞船相对平台的水平速度
shaping = ...
          - 150 * abs(rel_vx)      # 迫使飞船匹配平台水平速度后再落地
```

**原理**：弹飞的本质是接触瞬间飞船与平台存在**水平相对速度** → 刚性碰撞产生反弹冲量。
只要让飞船学会"先匹配平台的水平移动速度，再落下"，接触时相对速度 ≈ 0，就自然不弹。
这一项让飞船从"回中器"进化为"**速度匹配跟踪器**"，是本拓展最关键的 reward insight。

### 2.5 工程踩坑记录

开发过程中遇到并解决了若干 Box2D / gymnasium 层面的坑，记录如下（也是报告亮点）：

1. **多 reset 段错误**：`LunarLander.reset()` 内部会调用一次 `self.step(0)`，若此时平台
   body 引用还指向已被销毁的旧 world，访问即段错误（C 层面，try 接不住）。解决：在调
   `super().reset()` 前先把 `self.ship = None`。
2. **平台不移动**：曾用 `ship.world is self.world` 判断 body 是否有效，但 Box2D Python
   绑定的 `body.world` 返回代理对象，`is` 比较恒为 False，导致 step 永远走"平台失效"
   分支、时间步不自增、平台静止。改用 `try: ship.position` 判断后修复。
3. **观测归一化一致性**：自建环境构造的 8 维状态必须与 `LunarLander.step` 的归一化
   完全一致（`x/(W/2)`、`vel*W/2/FPS`、`20*angVel/FPS`），否则预训练策略失效。
4. **初始随机力误用**：原版 `INITIAL_RANDOM=1000` 是 `ApplyForceToCenter` 的力，不是
   速度；曾误当作速度赋给飞船，导致 ~150 m/s 初速直接飞出界。

---

## 三、算法与训练

本实验**基于 Stable-Baselines3（SB3）标准框架**实现算法。我们的核心创新在**自定义环境**
（移动平台 + reward 重塑，见第二节），算法的训练逻辑交给 SB3 的工业级实现，通过
gymnasium 标准接口接入。这样代码统一、可复现，也更符合业界惯例。

### 3.1 框架与接口

- **SB3 源码**：clone 官方仓库 `DLR-RM/stable-baselines3`（v2.9.0）到项目内并以可编辑模式
  安装（`pip install -e .`），便于后续按需修改算法实现。
- **统一调用接口**：

```python
from stable_baselines3 import DQN, PPO, A2C
env = MovingPadLunarLander()          # 我们的自定义环境（核心创新）
model = DQN("MlpPolicy", env)         # 或 PPO / A2C，SB3 标准接口
model.learn(total_timesteps=200000)   # 训练（SB3 内部封装全部逻辑）
action, _ = model.predict(obs)        # 推理
```

### 3.2 三种算法配置

在同一移动平台环境（amp=2.2）、同一奖励函数下，用 SB3 训练三种代表性算法：

| 算法 | 类型 | SB3 类 | 说明 |
|---|---|---|---|
| DQN | 深度值函数（off-policy） | `stable_baselines3.DQN` | 经验回放 + 目标网络 |
| PPO | 策略优化（on-policy） | `stable_baselines3.PPO` | clipped objective + GAE，当前 SOTA 之一 |
| A2C | 策略+值函数（on-policy） | `stable_baselines3.A2C` | 同步的 advantage actor-critic |

- **统一训练规模**：每个算法 200,000 timesteps，`MlpPolicy`（默认两层 64 网络或同类），
  CPU 训练，相同随机种子起点。
- **评估**：训练后用 SB3 的 `evaluate_policy` 在 10 个固定评估 episode 上做贪心策略评估。
- 训练产物见 `outputs/moving_pad/sb3/{dqn, ppo, a2c}/`（含 `model.zip`、`monitor.monitor.csv`
  训练曲线、`metrics.json` 评估结果）。

---


## 四、实验结果

### 4.1 训练收敛

三种 SB3 算法在相同环境与奖励下各训练 200,000 步，训练曲线见
`compare/sb3_training_curves.png`（基于 SB3 Monitor 的 episode-reward 记录）。
其中 **PPO 收敛最好**，评估平均 return 达 +888；A2C 次之（+41）；DQN 在大范围连续
控制任务上表现最差且方差大（−196）。值得注意的是，SB3 的 PPO 实现远强于我们早期
手写的 PPO（后者 eval 为负），说明**算法实现的工程质量对结果影响巨大**，工业级框架
的优势在此充分体现。

### 4.2 演示效果

扫描多个随机种子，挑选飞船**真实平稳**落到移动平台上的成功案例生成 GIF
（"真实平稳"判定：飞船落到甲板后**自身绝对速度 < 0.6 m/s**，稳定停留 40+ 帧，
且无反弹——非任何强制冻结）。

最佳案例（seed 154）：

| 指标 | 数值 |
|---|---|
| 落点相对平台中心偏差 | **0.007 m**（几乎正中） |
| 着陆姿态角 | **0.001 rad**（完全竖直） |
| 落上后稳定停留 | 92 帧 |
| 反弹 | 无 |

演示 GIF：`moving_pad_dqn_landing_4.gif`

### 4.3 平台速度的影响（鲁棒性分析）

我们对不同平台速度（`omega`）做了对比，观察"任务难度"与"可学习性"的权衡：

| 平台速度 | omega | 现象 |
|---|---|---|
| 慢速 | 0.6–0.8 | 飞船易跟踪，落准率高，但平台移动不明显 |
| 中速 | 1.1–1.2 | 较好平衡，落准率尚可，移动可见 |
| 1.3 倍速 | 1.2–1.6 | 窄范围（amp1.3）下配合速度惩罚可真平稳落地（见 4.2 演示） |
| 2 倍速 | 1.8–2.4 | 落准率明显下降，飞船易撞边弹飞 |
| 3 倍速 | 2.7–3.6 | **物理上飞船无法跟踪**，策略选择放弃降落、全程悬停 |

**注**：4.2 节的演示 GIF 与上表是在**窄移动范围（amp=1.3）**下取得的最佳平稳着陆
案例；本报告最终的算法对比（4.4 节）采用**大移动范围（amp=2.2，平台覆盖画面大部分
宽度）**，难度更高，用于公平考察各算法在更大动态范围下的表现。两套设置共同刻画了
"移动范围越大、任务越难"的趋势。

**关键发现**：平台速度存在一个"可学习上限"。当平台峰值水平速度（`amp × omega`）超过
飞船自身的水平机动能力时，飞船物理上无法在接触前匹配平台速度，于是策略学到"悬停避险"
而非"跟踪降落"。这是 sim-to-real / 任务难度边界的实证。

### 4.4 三种 SB3 算法对比

在统一的移动平台环境（amp=2.2，含水平相对速度惩罚奖励）下，用 SB3 的
`evaluate_policy` 对三种算法各做 10 个评估 episode 的贪心策略评估，结果如下
（柱状图见 `compare/sb3_eval_compare.png`）：

| 算法（SB3） | 类型 | 评估 mean return | 标准差 | 评价 |
|---|---|---:|---:|---|
| **PPO** | 策略优化（on-policy） | **+888.1** | 298.5 | 明显最优且较稳定 |
| A2C | 策略+值函数（on-policy） | +41.4 | 275.0 | 中等，勉强为正 |
| DQN | 深度值函数（off-policy） | −196.6 | 796.4 | 方差极大，最差 |

**为什么是这个结果？——三种算法的机制差异分析**

这一结果不是偶然，而是三种算法的**学习机制**与本任务特性的匹配程度决定的。下面从
机制层面逐个解释。

#### PPO 为什么最优（+888）

PPO（Proximal Policy Optimization）是当前 on-policy 策略优化的 SOTA，它在本任务胜出
源于三个机制优势：

1. **直接优化策略、输出随机性**：PPO 学习一个**概率策略** π(a|s)（输出 4 个动作的概率
   分布），而不是 Q 值。这意味着飞船可以"软"地混合动作（如 70% 主推 + 30% 右推），
   产生**平滑的连续控制**——这对"精确匹配平台水平速度"至关重要。而值方法（DQN）只能
   硬选一个动作，控制天然更抖。
2. **clipped objective + GAE**：PPO 用裁剪目标限制每次更新幅度（防止策略突变），用
   广义优势估计（GAE）平衡偏差与方差。这两者让策略梯度方向**又稳又准**，在"成功接触
   平台"这种稀疏正样本上能稳定地把策略往好方向推。
3. **on-policy + 多 epoch 复用**：PPO 用当前策略采的 rollout 数据做多个 epoch 的小步
   更新，样本利用充分，且因为裁剪保护，不易崩溃。

简言之，**PPO 的"平滑随机策略 + 稳定梯度"恰好契合"动态目标精细跟踪"** —— 这是它大幅
领先的结构性原因。

#### DQN 为什么最差（−196，方差 796）

DQN 在本任务表现差，同样是其机制决定的：

1. **离散硬选动作 → 控制抖动**：DQN 对每个状态输出 4 个 Q 值，**贪婪地选最大的一个**
   （ε-贪婪）。它无法"混合"动作，飞船控制是一连串跳变（主推↔右推↔不喷），这种抖动
   让飞船姿态和落点都极不稳定。GIF 里看到的"左右摇摆找平台"其实是这种抖动的**视觉
   表现**，并非主动跟踪。
2. **Q 值过估计**：DQN 的 max 操作会系统性高估 Q 值，在移动平台这种**高方差、稀疏成功**
   的任务里，过估计会让 agent 过度自信地选择次优动作，方差被放大（796 的标准差正源于此）。
3. **off-policy + 经验回放在动态任务里失配**：DQN 反复回放旧经验。但移动平台**平台相位
   每局不同**（状态分布非平稳），旧经验里"平台在左"的样本，回放时可能误导当前"平台
   在右"的决策。PPO 的 on-policy 没有这个问题。

> 注：DQN 的弱点**不是"它是 DQN"，而是"离散值方法在连续精细控制上的固有劣势"**。在
> 原版固定平台 LunarLander 上 DQN 表现尚可（任务简单、容差大）；移动平台要求精确速度
> 匹配，DQN 的抖动被放大成致命缺陷。

#### A2C 为什么居中（+41）

A2C（Advantage Actor-Critic）和 PPO 同属 on-policy 策略梯度，机制相近，但有两个关键
差距让它落在中间：

1. **没有 clipping 保护**：A2C 是"原版"策略梯度，每次更新直接用 advantage，没有 PPO 的
   裁剪。策略更新幅度大、易震荡，在难任务上不如 PPO 稳定。
2. **同步、无多 epoch 复用**：A2C 用完一批数据就丢，不像 PPO 多 epoch 复用。样本效率
   低于 PPO。

所以 A2C 学到了一些（+41，为正），但远不如 PPO 精炼。**A2C ≈ "没有 PPO 那些工程保护
的简化版 PPO"**，性能也恰好在 PPO 和失败之间。

#### 机制层面的统一解释

把三者放在一起看，能提炼出一条规律：

> **本任务（连续精细控制 + 稀疏成功 + 动态目标）奖励"平滑、稳定、样本高效"的算法。**
> **PPO 同时满足这三点 → 最优；A2C 少了稳定性 → 中等；DQN 三点都缺 → 最差。**

这也解释了为什么"手写版"对比时 DQN 反而最好（见附录）：那是因为我们手写的 PPO/A2C
**实现质量太差**（缺观测归一化、rollout 太短），没能发挥策略梯度的真实水平；换用
工业级 SB3 实现后，算法机制的真实排序才显现出来。**这说明 RL 算法对比的结论高度依赖
实现质量，必须用标准框架才公平。**

### 4.5 重要发现：高 return ≠ 学到了"真跟踪"

我们对各算法策略做更深入的行为分析：记录每个评估 episode 中**飞船的水平位置 x 与平台
水平位置 x 的相关性**。若飞船真正在"跟踪"平台，两者应正相关（平台右移飞船也右移）。

测量结果（取接近降落阶段的 200 步）：

| 算法 / seed | 飞船 x 与平台 x 的相关系数 |
|---|---:|
| PPO seed7 | +0.07（几乎不相关） |
| PPO seed8 | −0.64（反向） |
| PPO seed55 | +0.68（正相关，像跟踪） |

**发现**：SB3-PPO 虽然整体 return 最高（+888），但**多数 seed 下飞船并不真正跟踪平台**
（相关性接近 0 甚至为负），它学到的是一种**取巧策略**——在某个区域竖直下降，依赖平台
移动范围大（amp=2.2）总能扫过飞船下方而"碰巧"落上。只有个别 seed（如 seed55）表现出
真正的同步跟踪。

这是一个有价值的深度 RL insight：**高的 episode return 并不保证策略学到了任务设计者期望
的"正确行为"**，策略可能通过 reward 的捷径（取巧）获得高分。要验证策略是否真学到目标
行为，必须做**行为层面的分析**（如本节的相关性分析），而不能只看 return。

演示 GIF（`gifs/`）：
- `sb3_ppo_landing.gif`：挑选了 PPO 中真正同步跟踪的 seed55（相关性 0.68），可看出
  飞船随平台左右移动而横向机动；
- `sb3_dqn_landing.gif`：DQN 的摇摆式跟踪；
- `sb3_a2c_landing.gif`：A2C 表现。

---

## 五、困难与讨论

### 5.1 主要困难

1. **弹飞问题**：高速平台下，飞船带水平速度撞甲板被弹开。最终通过"水平相对速度惩罚"
   让飞船学会速度匹配后解决（而非靠摩擦或强制冻结等"作弊"手段）。
2. **平稳 vs 速度的权衡**：平台越快，越难平稳落准。3 倍速在物理上不可学。
3. **策略成功率不高**：即便最终版本，随机种子下的落准率仍有限（约百分之几到十几），
   需扫描多个种子挑出成功案例。这是动态目标跟踪任务固有的难度。
4. **PPO 不稳定**：PPO 在训练中能偶发高 return，但统一评估时显著弱于 DQN，说明策略没有
   稳定掌握"跟踪平台 + 匹配水平速度 + 接触控制"这一组合行为。

### 5.2 与基础任务的对比（insight）

| 维度 | 基础 LunarLander | 移动平台（本拓展） |
|---|---|---|
| 目标 | 静态（画面中心） | 动态（往返移动） |
| 策略本质 | 回中器 | 速度匹配跟踪器 |
| 观测 | 8 维 | 10 维（+平台位置） |
| 接触控制 | 落地即可 | 需近零相对速度，否则弹飞 |
| reward 关键项 | 距离 + 速度 + 姿态 | + **水平相对速度惩罚** |

**核心 insight**：把目标从静态变动态，看似只是"多了个移动平台"，实则对智能体提出了
本质更高的要求——**不仅要控制自身运动，还要预测并匹配外部目标的运动**。基础任务的
reward 框架（距离 + 速度 + 姿态）不足以让智能体学会这一点，必须显式引入"相对速度"
这一**关系量**作为奖励信号。这一发现可推广到其他"动态目标跟踪"任务（如机械臂抓取
移动物体、无人机拦截）。

### 5.3 局限与未来工作

- **成功率**：当前随机种子下落准率有限，可通过更长训练、优先经验回放（PER）或更大网络
  容量提升；
- **平台运动模型**：目前是确定性正弦，可扩展为随机过程（更接近真实海浪），考察策略
  泛化与鲁棒性；
- **域随机化**：训练时随机化平台速度/幅度，有望得到对未知运动更鲁棒的策略。

---

## 六、文件清单

```
outputs/moving_pad/
├── REPORT.md                      本报告
├── sb3/                           ★ SB3 三算法训练产物
│   ├── dqn/  (model.zip, monitor.monitor.csv, metrics.json)
│   ├── ppo/  (model.zip, monitor.monitor.csv, metrics.json)
│   └── a2c/  (model.zip, monitor.monitor.csv, metrics.json)
├── gifs/                          所有演示 GIF（含 sb3_*_landing.gif 三算法对比）
├── compare/                       对比图（sb3_eval_compare.png 柱状图、sb3_training_curves.png 训练曲线）
└── logs/                          训练日志
```

环境与训练代码：

```
lunar_lander_rl/
├── moving_pad_env.py              移动平台环境（实体平台 + maskBits + reward 重塑）★ 核心创新
├── sb3_train.py                   ★ SB3 训练（DQN/PPO/A2C 统一接口）
├── sb3_demo.py                    ★ SB3 演示 GIF 生成
├── sb3_plot.py                    SB3 eval 柱状图
├── sb3_curves.py                  SB3 训练曲线图
└── common.py                      环境注册 register_moving_pad / make_env
stable-baselines3/                 SB3 源码（clone + pip install -e .，可修改）
```
```

---

## 七、复现命令

```bash
cd Lunar-Lander-Plus

# 0. 安装 SB3（已 clone 源码到 stable-baselines3/，可编辑模式）
pip install -e ./stable-baselines3

# 1. 三种算法在统一移动平台环境（amp=2.2）下训练（各 200k 步）
python -m lunar_lander_rl.sb3_train --algo dqn --timesteps 200000
python -m lunar_lander_rl.sb3_train --algo ppo --timesteps 200000
python -m lunar_lander_rl.sb3_train --algo a2c --timesteps 200000

# 2. 生成三算法演示 GIF
python -m lunar_lander_rl.sb3_demo

# 3. 生成对比图（柱状图 + 训练曲线）
python -m lunar_lander_rl.sb3_plot
python -m lunar_lander_rl.sb3_curves
```

---
```

---

## 附录：手写实现 vs SB3 实现的对比（算法对比公平性）

在采用 SB3 之前，我们曾用**自己从零手写**的 DQN / PPO / Actor-Critic / Q-Learning 在
同一移动平台环境上做对比，结果与 SB3 版**完全相反**：

| 框架 | DQN | PPO | A2C/AC | Q-Learning |
|---|---|---|---|---|
| 手写（从零） | **+735（最优）** | −364 | −667 | −435 |
| SB3（工业级） | −196（最差） | **+888（最优）** | +41 | — |

**结论完全反转**：手写版 DQN 最优，SB3 版 DQN 最差。

**原因**：这不是算法变了，而是**实现质量**变了。我们手写的 PPO/A2C 是"能跑的最简实现"，
缺少观测归一化、rollout 太短、value clipping、advantage 标准化等关键工程；而手写 DQN
逻辑简单稳定。结果手写版比的是"谁的实现更完整"，而非"谁的算法思想更适合本任务"。

换用 SB3（工业级、各算法实现质量一致）后，算法思想的真实排序才显现：**PPO > A2C > DQN**。

**方法论启示**：强化学习的算法对比，**结论高度依赖实现质量**。手写实现的质量差异会
污染对比结论。因此严肃的算法对比应使用标准框架（SB3 / CleanRL 等），保证各算法实现
质量一致——这正是本报告最终采用 SB3 的原因。
